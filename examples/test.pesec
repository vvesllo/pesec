use "std/io.dll";
use "std/types.dll";


mutab i = 0;
const a = while i < 10
{
    i += 10;
    println(i);
    return if i > 2
    {
        return i * 2;
    }
    else 
    {
        return i - 100;
    };
} else {
    mutab b = 10;
    println("Breakn't " + tostring(b));
};

println(a);