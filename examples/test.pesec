use "build/stdlib/io/Release/io.dll";
use "build/stdlib/types/Release/types.dll";

print("accumulate [1...10] = ");
println(fn(arr, f) {
    mut res = 0;
    for i in arr {
        res += f(i);
    };
    return res;
} (1...100, fn(a) {
    return a ** 3;
}));