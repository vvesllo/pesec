const cstdio = import "std/io/libio.so";

const print = fn(args) {
    for arg in args
        cstdio.cout(arg);
};

const input = fn(args) {
    for arg in args
        cstdio.cout(arg);
    cstdio.cin();
};